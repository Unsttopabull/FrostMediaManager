<UserControl
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
    xmlns:in="http://schemas.microsoft.com/expression/2010/interactions"
    xmlns:uc="clr-namespace:RibbonUI.UserControls"
    x:Name="RibbonControl" x:Class="RibbonUI.UserControls.Ribbon"
    mc:Ignorable="d" d:DesignHeight="140" d:DesignWidth="700">
    <UserControl.DataContext>
        <uc:RibbonViewModel/>
    </UserControl.DataContext>
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding OnRibbonLoadedCommand}" CommandParameter="{Binding ElementName=RibbonControl}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Ribbon x:Name="RibbonHeader" Title="Frost Media Manager" Background="#FFDFE9F5">
        <Ribbon.ContextualTabGroups>
            <RibbonContextualTabGroup Background="Red" x:Name="ContextSubtitle" Visibility="{Binding SubtitlesContextVisible}" Header="Subtitles" />
        </Ribbon.ContextualTabGroups>

        <Ribbon.ApplicationMenu>
            <RibbonApplicationMenu Visibility="Collapsed">
            </RibbonApplicationMenu>
        </Ribbon.ApplicationMenu>

        <RibbonTab IsSelected="{Binding IsSearchTabSelected}" Header="Search" Margin="0,1,0,-1">
            <RibbonGroup Header="Searching &amp; Detection">
                <RibbonButton LargeImageSource="../Images/search.ico" Label="Search for new movies" Command="{Binding SearchCommand}" />
                <RibbonButton SmallImageSource="../Images/refresh.png" Label="Update all" IsEnabled="False"/>
                <RibbonButton SmallImageSource="../Images/export.png" Label="Export movies" Command="{Binding ExportMoviesCommand}"/>
            </RibbonGroup>
            <RibbonGroup Header="Current movie">
                <RibbonButton x:Name="OpenInFolder" x:FieldModifier="private"
                              LargeImageSource="../Images/movieFolder.ico" Label="Open movie folder"
                              ToolTip="{Binding SelectedMovie.DirectoryPath}"
                              Command="{Binding OpenMovieInFolderCommand}" />
                <RibbonButton SmallImageSource="../Images/play.png" Label="Play movie" Command="{Binding PlayMovieCommand}"/>
                <RibbonButton SmallImageSource="../Images/refresh.png" Label="Refresh detection" IsEnabled="false"/>
                <RibbonButton SmallImageSource="../Images/remove2.png" Label="Remove movie" />
            </RibbonGroup>
            <RibbonGroup>
                <RibbonButton LargeImageSource="../Images/save.png" Label="Save changes" Command="{Binding SaveChangesCommand}"/>
            </RibbonGroup>
            <RibbonGroup>
                <RibbonButton LargeImageSource="../Images/options.png" Label="Options" Command="{Binding OptionsCommand}"/>
            </RibbonGroup>            
        </RibbonTab>

        <RibbonTab IsSelected="{Binding IsDetectTabSelected}" Header="Web update">
            <RibbonGroup Header="Movie info" Background="#00000000" Height="86" Margin="0">
                <RibbonMenuButton x:Name="MovieInfoDownloaders" LargeImageSource="../Images/update.png" Label="Update movie information"/>
            </RibbonGroup>
            <RibbonGroup Header="Art">
                <RibbonMenuButton x:Name="MovieArtDownloaders" LargeImageSource="../Images/art.png" Label="Download art"/>
                <RibbonMenuButton LargeImageSource="../Images/download4.png" Label="Save current art locally">
                    <RibbonMenuItem Header="For current movie"/>
                    <RibbonMenuItem Header="For all movies"/>
                </RibbonMenuButton>
            </RibbonGroup>
            <RibbonGroup>
                <RibbonMenuButton x:Name="MovieVideoDownloaders" LargeImageSource="../Images/video.png" Label="Download promotional videos" IsEnabled="{Binding SelectedMovie[PromotionalVideos]}"/>
            </RibbonGroup>
        </RibbonTab>

        <RibbonTab x:Name="SubtitlesTab" IsSelected="{Binding IsSubtitlesTabSelected}" ContextualTabGroupHeader="Subtitles">
            <RibbonGroup Header="Subtitles" IsEnabled="True">
                <RibbonMenuButton Label="Download" LargeImageSource="../Images/add.png">
                    <RibbonMenuItem ImageSource="../Images/sites/podnapisiNet.ico" Header="Download from Podnapisi.NET" Command="{Binding DownloadSubtitlesCommand}" CommandParameter="{x:Static uc:SubtitleSites.PodnapisiNet}"/>
                    <RibbonMenuItem ImageSource="../Images/sites/opensubtitles.ico" Header="Download from OpenSubtitles.org" Command="{Binding DownloadSubtitlesCommand}" CommandParameter="{x:Static uc:SubtitleSites.OpenSubtitles}"/>
                </RibbonMenuButton>
                <RibbonButton Label="Detect Language" LargeImageSource="../Images/search_subs.ico" />
            </RibbonGroup>
        </RibbonTab>
    </Ribbon>
</UserControl>