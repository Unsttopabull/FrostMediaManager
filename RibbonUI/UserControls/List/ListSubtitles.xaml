<UserControl x:Class="RibbonUI.UserControls.List.ListSubtitles"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:files="clr-namespace:Frost.Common.Models.DB.MovieVo.Files;assembly=Frost.Common"
             xmlns:converters="clr-namespace:RibbonUI.Converters"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="602">
    <UserControl.Resources>
        <converters:BooleanToYesNoConverter x:Key="BooleanToYesNoConverter"/>
        <BitmapImage x:Key="UnknownLanguage" UriSource="../../Images/unknown_large.png" />
    </UserControl.Resources>
    <ListView x:Name="SubtitlesList" SelectionMode="Single" ItemsSource="{Binding}" ClipToBounds="True">
        <ListView.GroupStyle>
            <GroupStyle ContainerStyle="{StaticResource ExpanderGroup}"/>
        </ListView.GroupStyle>        
        <ListView.Resources>
            <Style TargetType="ListViewItem">
                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            </Style>
        </ListView.Resources>
        <ListView.ItemTemplate>
            <DataTemplate DataType="files:Subtitle">
                <DockPanel LastChildFill="False">
                    <StackPanel DockPanel.Dock="Left">
                        <Image Source="{Binding Language.ISO639.Alpha3, Converter={StaticResource PathToImageSourceConverter}, ConverterParameter=Language, FallbackValue={StaticResource UnknownLanguageLarge}, TargetNullValue={StaticResource UnknownLanguageLarge}}"
                               MinHeight="48" MaxHeight="100">
                            <Image.CacheMode>
                                <BitmapCache />
                            </Image.CacheMode>
                        </Image>
                    </StackPanel>
                    <StackPanel Margin="15,0,0,0" MinWidth="200" x:Name="DetailsHolder">
                        <StackPanel Orientation="Horizontal" Tag="{Binding Language.Name}" Style="{StaticResource CollapseOnTagNull}">
                            <Label>Language:</Label>
                            <TextBlock VerticalAlignment="Center" Text="{Binding Language.Name}" />
                        </StackPanel>                        
                        <StackPanel Orientation="Horizontal" Tag="{Binding Format}" Style="{StaticResource CollapseOnTagNull}">
                            <Label>Format:</Label>
                            <TextBlock VerticalAlignment="Center" Text="{Binding Format}" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Tag="{Binding Encoding}" Style="{StaticResource CollapseOnTagNull}">
                            <Label>Encoding:</Label>
                            <TextBlock VerticalAlignment="Center" Text="{Binding Encoding}" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <Label>Is embeded in the video:</Label>
                            <TextBlock VerticalAlignment="Center" Text="{Binding EmbededInVideo, Converter={StaticResource BooleanToYesNoConverter}}" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <Label>Is for hearing impaired:</Label>
                            <TextBlock VerticalAlignment="Center" Text="{Binding ForHearingImpaired, Converter={StaticResource BooleanToYesNoConverter}}" />
                        </StackPanel>
                    </StackPanel>

                    <StackPanel DockPanel.Dock="Right">
                        <Button Click="OnRemoveClicked" Style="{StaticResource TransparentBorderlessButton}">
                            <Image Source="../../Images/remove.png" />
                        </Button>
                        <Button Click="OnEditClicked" Style="{StaticResource TransparentBorderlessButton}">
                            <Image Source="../../Images/edit.ico" />
                        </Button>
                    </StackPanel>
                </DockPanel>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</UserControl>