<UserControl x:Name="userControl" x:Class="RibbonUI.EditMovie"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="625" d:DesignWidth="1071">
    <Grid Margin="5,10,5,5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" MinWidth="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel>
            <Label Target="{Binding ElementName=MovieTitle}">Title:</Label>
            <TextBox x:Name="MovieTitle" Text="{Binding ElementName=userControl, Path=Movie.Title}"/>
            <Label Target="{Binding ElementName=MovieOriginalTitle}">Original title:</Label>
            <TextBox x:Name="MovieOriginalTitle" Text="{Binding ElementName=userControl, Path=Movie.OriginalTitle}"/>
        </StackPanel>
    </Grid>
</UserControl>
